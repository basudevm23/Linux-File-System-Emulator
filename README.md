Built a Linux-inspired file system emulator in C++ implementing file and directory operations, inode-based storage, hierarchical traversal, and permission management with custom data structures

Designed a virtual disk layer with block allocation, free-space management, metadata persistence, and buffered I/O, simulating core operating-system file system mechanisms

Applied OOP, trees, hashing, memory management, and exception handling to support concurrent file operations and maintain file-system consistency
